name: "md_pipeline_demo"
version: "1.0.0"
config-version: 2
profile: "md_pipeline_demo"

model-paths: ["models"]
macro-paths: ["macros"]
clean-targets: ["target", "dbt_packages"]

models:
  md_pipeline_demo:
    staging:
      +materialized: view
      +schema: staging
    marts:
      +materialized: table
      +schema: analytics
